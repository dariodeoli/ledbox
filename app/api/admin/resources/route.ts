import { randomUUID } from "node:crypto";
import { requireAdmin } from "@/lib/server/auth";
import { db } from "@/lib/server/db";
import { jsonError, readJson } from "@/lib/server/http";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function GET() { if (!(await requireAdmin())) return jsonError("Unauthorized", 401); const [suppliers, inventory, promoters] = await Promise.all([db.supplier.findMany({ orderBy: { name: "asc" }, take: 200 }), db.inventoryItem.findMany({ orderBy: { name: "asc" }, take: 300 }), db.promoter.findMany({ where: { active: true }, orderBy: { name: "asc" }, take: 200 })]); return Response.json({ suppliers, inventory, promoters }); }
export async function POST(request: Request) { if (!(await requireAdmin())) return jsonError("Unauthorized", 401); const body = await readJson(request) as Record<string, unknown>; const kind = body.kind; if (kind === "supplier" && typeof body.name === "string") return Response.json({ supplier: await db.supplier.create({ data: { id: randomUUID(), name: body.name.trim(), company: typeof body.company === "string" ? body.company.trim() : undefined, phone: typeof body.phone === "string" ? body.phone.trim() : undefined, category: "OTHER" } }) }, { status: 201 }); if (kind === "inventory" && typeof body.name === "string") return Response.json({ inventory: await db.inventoryItem.create({ data: { id: randomUUID(), name: body.name.trim(), category: typeof body.category === "string" ? body.category.trim() : "General", kind: body.inventoryKind === "CONSUMABLE" ? "CONSUMABLE" : body.inventoryKind === "DISPOSABLE" ? "DISPOSABLE" : "REUSABLE", quantity: typeof body.quantity === "number" ? body.quantity : 1 } }) }, { status: 201 }); if (kind === "promoter" && typeof body.name === "string") return Response.json({ promoter: await db.promoter.create({ data: { id: randomUUID(), name: body.name.trim(), phone: typeof body.phone === "string" ? body.phone.trim() : undefined, specialties: typeof body.specialties === "string" ? body.specialties.trim() : undefined } }) }, { status: 201 }); return jsonError("Invalid resource.", 400); }
